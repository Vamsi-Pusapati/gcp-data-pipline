apiVersion: v1
kind: Service
metadata:
  name: medicaid-dashboard-svc
  labels:
    app: medicaid-dashboard
spec:
  type: LoadBalancer
  selector:
    app: medicaid-dashboard
  ports:
  - port: 80
    targetPort: 8501
    protocol: TCP
    name: http
  sessionAffinity: ClientIP
